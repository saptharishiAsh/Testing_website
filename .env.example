# Copy these values into your shell or a local .env loader before starting the server.
# Never commit the real API key.
RESEND_API_KEY=re_your_api_key_here
EMAIL_FROM=Rishi Mart <onboarding@resend.dev>
PORT=8000
